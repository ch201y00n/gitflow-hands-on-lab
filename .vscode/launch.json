{
  "version": "0.2.0",
  "configurations": [
    {
      "name": ".NET Launch (gitflow-hands-on-lab)",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "dotnet build",
      "program": "${workspaceFolder}/bin/Debug/net9.0/gitflow-hands-on-lab.dll",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
